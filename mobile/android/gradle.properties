org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# audioplayers_android 5.3.0 still applies the legacy kotlin-android plugin
# internally, which AGP 9 rejects when builtInKotlin=true. Setting false
# restores the old behaviour for all subprojects until audioplayers_android
# migrates to built-in Kotlin (tracked upstream).
android.builtInKotlin=false
